# Syncronising files to other repos

Files in the `/synced-files` folder and mentioned in the `.github/sync.yml` file will be syncronised via PR's to selected repos when PR's are merged to main in this repo.
